rows = select * from table_name where productCode=${selectedProductCode}

The above query will respond with at least one row with revision No. "OE".
It may return multiple rows with revision No. "OE", "01","02", "08"

lastRow = rows[rows.length-1];
lastRevisionNo = lastRow[revisionNo];  => OE

if(isNaN(lastRevisionNo))